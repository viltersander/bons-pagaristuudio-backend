import type { NavigateFunction } from "react-router-dom";
declare type NotificationFunction = (title: string, message: string) => void;
export declare type Notify = {
    error: NotificationFunction;
    success: NotificationFunction;
    info: NotificationFunction;
    warning: NotificationFunction;
};
export declare type Navigate = NavigateFunction;
export declare type FeatureFlagHelpers = {
    isFeatureEnabled: (flag: string) => boolean;
    featureToggleList: Record<string, boolean>;
};
export {};
